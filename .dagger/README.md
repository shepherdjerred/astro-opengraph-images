# Astro OpenGraph Images – Dagger Module

This directory contains the Dagger module that powers the continuous
integration pipeline for the Astro OpenGraph Images project.

## Usage

```bash
# list available functions
bunx dagger functions

# run the complete CI pipeline
bunx dagger call ci --source=.
```

Refer to `DAGGER_MIGRATION.md` in the repository root for more
information about the migration and available commands.
